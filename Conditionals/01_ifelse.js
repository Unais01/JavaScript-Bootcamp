const driverAge = 2;

if (driverAge > 18 && driverAge < 60) {
    console.log("he/she can drive");
} else {
    console.log("don't have permit to drive");
}

driverAge > 18 ? console.log("drive") : console.log("don't");
